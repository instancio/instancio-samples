# Instancio Sample Gradle Project

Includes examples of `InstancioExtention` for JUnit 5 and metamodels
generated using `@InstancioMetamodel` annotation.

```
cd instancio-gradle-sample
./gradlew build
```
